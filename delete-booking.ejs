<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/styles.css">
    <title>Delete Booking</title>
</head>
<body>
    <header>
        <h1>Delete Booking</h1>
    </header>
    <main>
        <div class="form-container">
            <h2>Confirm Deletion</h2>
            <p>Are you sure you want to delete the booking for:</p>
            <p><strong>Name:</strong> <%= booking.name %></p>
            <p><strong>Date:</strong> <%= booking.date.toISOString().split('T')[0] %></p>
            <p><strong>Time:</strong> <%= booking.time %></p>
            <p><strong>Payment:</strong> <%= booking.payment %> Euros</p>
            <form action="/delete-booking/<%= booking._id %>" method="POST">
                <button type="submit">Delete Booking</button>
                <a href="/">Back to Home</a>
            </form>
        </div>
    </main>
</body>
</html>
